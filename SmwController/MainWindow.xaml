<Window x:Class="SmwController.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SmwController.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:MainViewModel}"
        Title="R&amp;S SMW200A Controller"
        MinWidth="700" MinHeight="600"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <vm:InverseBoolConverter x:Key="InvBool"/>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="Foreground" Value="#2C5F8A"/>
        </Style>

        <Style x:Key="GroupBox" TargetType="Border">
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Background" Value="#FAFAFA"/>
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ── Connection ── -->
        <Border Grid.Row="0" Style="{StaticResource GroupBox}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Style="{StaticResource SectionHeader}" Text="Connection"/>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="190"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" Content="IP Address:" Margin="0,0,4,0"/>
                    <TextBox Grid.Column="1"
                             Text="{Binding IpAddress, UpdateSourceTrigger=PropertyChanged}"
                             IsEnabled="{Binding IsBusy, Converter={StaticResource InvBool}}"/>

                    <Label Grid.Column="2" Content="Port:" Margin="10,0,4,0"/>
                    <TextBox Grid.Column="3"
                             Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}"
                             IsEnabled="{Binding IsBusy, Converter={StaticResource InvBool}}"/>

                    <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Connect"    Command="{Binding ConnectCommand}"/>
                        <Button Content="Disconnect" Command="{Binding DisconnectCommand}"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- ── Waveform Browser ── -->
        <Border Grid.Row="1" Style="{StaticResource GroupBox}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Style="{StaticResource SectionHeader}" Text="Waveform Browser"/>

                <!-- Root path + Scan -->
                <Grid Grid.Row="1" Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Content="Root path:" Margin="0,0,4,0"/>
                    <TextBox Grid.Column="1"
                             Text="{Binding ScanRootPath, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,6,0"/>
                    <Button Grid.Column="2"
                            Content="Scan Instrument"
                            Command="{Binding ScanWaveformsCommand}"/>
                </Grid>

                <!-- Search / filter -->
                <Grid Grid.Row="2" Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Content="Search:" Margin="0,0,4,0"/>
                    <!-- Grid overlay provides the placeholder hint text -->
                    <Grid Grid.Column="1">
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 IsEnabled="{Binding IsBusy, Converter={StaticResource InvBool}}"/>
                        <TextBlock Text="Filter by name or path…"
                                   Foreground="#AAAAAA"
                                   IsHitTestVisible="False"
                                   Margin="7,0,0,0"
                                   VerticalAlignment="Center"
                                   FontSize="13">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SearchText}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Grid>

                <!-- Waveform list -->
                <ListView Grid.Row="3"
                          ItemsSource="{Binding FilteredWaveformFiles}"
                          SelectedItem="{Binding SelectedWaveform}"
                          BorderBrush="#CCCCCC"
                          BorderThickness="1"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          VirtualizingPanel.IsVirtualizing="True">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Directory" Width="330">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource PathDirectoryConverter}}"
                                                   ToolTip="{Binding}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="File" Width="240">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource PathFilenameConverter}}"
                                                   ToolTip="{Binding}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>

                <!-- Selection label + Load button -->
                <Grid Grid.Row="4" Margin="0,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"
                               Text="{Binding SelectedWaveform, TargetNullValue='No waveform selected.'}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#333333"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedWaveform}" Value="{x:Null}">
                                        <Setter Property="Foreground" Value="#AAAAAA"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Button Grid.Column="1"
                            Content="Load Selected Waveform"
                            Command="{Binding LoadWaveformCommand}"/>
                </Grid>
            </Grid>
        </Border>

        <!-- ── RF Output Control ── -->
        <Border Grid.Row="2" Style="{StaticResource GroupBox}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Style="{StaticResource SectionHeader}" Text="RF Output Control"/>

                <!-- Ramp parameters (read-only display) -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Start: " Foreground="#555"/>
                    <TextBlock Text="-60 dBm" FontWeight="SemiBold" Margin="0,0,16,0"/>
                    <TextBlock Text="Stop: "  Foreground="#555"/>
                    <TextBlock Text="0 dBm"   FontWeight="SemiBold" Margin="0,0,16,0"/>
                    <TextBlock Text="Step: "  Foreground="#555"/>
                    <TextBlock Text="10 dBm"  FontWeight="SemiBold" Margin="0,0,16,0"/>
                    <TextBlock Text="Dwell: " Foreground="#555"/>
                    <TextBlock Text="10 s"    FontWeight="SemiBold"/>
                </StackPanel>

                <!-- Buttons + live level display -->
                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <Button Content="▶  Start RF"
                            Command="{Binding StartRfCommand}"
                            Background="#D4EDDA"
                            BorderBrush="#28A745"/>

                    <Button Content="■  Stop RF"
                            Command="{Binding StopRfCommand}"
                            Background="#F8D7DA"
                            BorderBrush="#DC3545"/>

                    <StackPanel Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Current Level: " VerticalAlignment="Center" Foreground="#555"/>
                        <TextBlock Text="{Binding CurrentLevel, StringFormat={}{0:+0.0;-0.0} dBm}"
                                   VerticalAlignment="Center"
                                   FontSize="15"
                                   FontWeight="Bold"
                                   Foreground="#2C5F8A"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ── Status Bar ── -->
        <Border Grid.Row="3"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10,8"
                Background="#F0F4F8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Status: "
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"/>

                <TextBlock Grid.Column="1"
                           Text="{Binding Status}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="10" Height="10" Margin="0,0,5,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#DC3545"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                        <Setter Property="Fill" Value="#28A745"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Disconnected"/>
                                <Setter Property="Foreground" Value="#DC3545"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                        <Setter Property="Text" Value="Connected"/>
                                        <Setter Property="Foreground" Value="#28A745"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Busy overlay -->
        <Grid Grid.Row="0" Grid.RowSpan="4"
              Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"
              Background="#50000000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="10" Margin="0,0,0,8"/>
                <TextBlock Text="Working…" Foreground="White" HorizontalAlignment="Center" FontSize="14"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
